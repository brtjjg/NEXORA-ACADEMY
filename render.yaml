services:
  - type: web
    name: nexora-academy
    env: node
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
    healthCheckPath: /health
    domains:
      - nexora-academy.onrender.com
